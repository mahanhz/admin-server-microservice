spring:
  application:
    name: admin-server-microservice
  cloud:
    config:
      enabled: true
      failFast: true
      retry:
        initialInterval: 5000
        maxInterval: 10000
        maxAttempts: 20